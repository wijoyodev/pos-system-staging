{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "php artisan migrate --force && php artisan storage:link --force && php artisan db:seed --force && php artisan serve --host=0.0.0.0 --port=$PORT && php artisan queue:work --sleep=3 --tries=3 --max-time=3600"
    }
}